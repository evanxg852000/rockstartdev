

class Stack(object):
    def __init(self):
        self._store = LinkedList()

    def push(self, data):
        self._store.add_back(data)

    def pop(self):
        back = self._store.back()
        remove

    def peek(self):
        return self._store.back()

    def items(self):
        return self._store.items()

    def count(self):
        return self._store.count()
